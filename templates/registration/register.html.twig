{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
<!-- Page Heading Section Start -->
    <div class="page-heading-section section bg-parallax" data-bg-image="assets/images/bg/bg-1.jpg" data-overlay="50">
        <div class="container">
            <div class="page-heading-content text-center">
                <h3 class="title">Register</h3>
            </div>
        </div>
    </div>
    <!-- Page Heading Section End -->
   
    <!-- Create Job Start -->
    <div class="section section-padding">
        <div class="container">
            <div class="row">
                <div class="create-job-form col-lg-10 mx-auto">
                    {% for flashError in app.flashes('verify_email_error') %}
                    <div class="alert alert-danger" role="alert">{{ flashError }}</div>
                    {% endfor %}
                    <h1 class="h3 mb-3 font-weight-normal">Please sign up</h1>
                        {{ form_start(registrationForm) }}
                            {{ form_row(registrationForm.email) }}<br>
                            {{ form_row(registrationForm.plainPassword, {
                                label: 'Password'
                            }) }}<br>
                            {{ form_row(registrationForm.CIN) }}<br>
                            {{ form_row(registrationForm.Telephone) }}<br>
                            {{ form_row(registrationForm.Nom_utilisateur) }}<br>
                            {{ form_widget(registrationForm) }}<br>
                            {{ form_row(registrationForm.agreeTerms) }}<br>
                            <button type="submit" class="btn btn-primary">Register</button>
                        {{ form_end(registrationForm) }}
                </div>
            </div>
        </div>
    </div>
<!-- Create Job End -->
{% endblock %}
