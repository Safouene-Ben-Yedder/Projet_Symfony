{% extends 'base.html.twig' %}

{% block title %}Modifier le profil{% endblock %}

{% block body %}
    <!-- Page Heading Section Start -->
    <div class="page-heading-section section bg-parallax" data-bg-image="{{asset('assets/images/bg/bg-1.jpg')}}" data-overlay="50">
        <div class="container">
            <div class="page-heading-content text-center">
                <h3 class="title">Modifier les informations du profil</h3>
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="">Accueil</a></li>
                    <li class="breadcrumb-item active">Mon profil</li>
                </ol>
            </div>
        </div>
    </div>

    <div class="section section-padding">
        <div class="container">
            <div class="row">
                <div class="col-lg-10 col-12 mx-auto">
                    <div class="faq-card card">
                            {{ include('user/_form.html.twig', {'button_label': 'Modifier'}) }}
                            {{ include('user/_delete_form.html.twig') }}
                            <a href="{{ path('user_show') }}">Retourner</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
